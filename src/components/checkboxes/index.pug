div(data-hide='parent').checkboxes
    div(data-hide='list').checkboxes__list
        -
            var name = arr.name
        each i,x in arr.list
           div(data-hidden=i.hidden class=i.hidden?'hidden':'').checkboxes__item
                include ../../components/checkbox/index
    if arr.more
        button(type='button' data-hide='btn').checkboxes__more.filter-block__more
            span Показать все
            span Скрыть